#include<math.h>  
#include <iostream>
#include <sstream>
using namespace std;
int main()
{
    setlocale(LC_ALL, "russian");
    cout << "input size of array" << endl;
    int n; cin >> n;
    int* mass = new int[n];
    cout << "input elements of array\n";
    for (size_t i = 0; i <n; i++)
    {
        cin >> mass[i];
    } 
    cout << "input number\n";
    int inputnumber; cin >> inputnumber;
    for (size_t i = 0; i < n; i++)
    {
        if (inputnumber > mass[i])
            cout << mass[i] << endl;
    } 
    system("pause");
    return 0;
} 
 